# ワセ飯 — 確定仕様書 v2.0

> 諸富へ：**この仕様に合わせてReact版を作ってほしい。**
> 太陽がHTML版で全機能を検証済み。数字・ルールはこれで確定。
> コードは別々（HTML / React）でいいが、**ルールはこの1つに統一する。**
>
> 検証用HTML版： https://exports-seven.vercel.app/waseda-meshi.html

---

## コンセプト

**「早稲田にいる今この瞬間」だけが見える、フレンド専用ご飯SNS**

BeReal でも Snapchat でもない。早稲田生が大学にいる間だけ使う閉じたコミュニティ。

---

## 📸 投稿

- **BeReal式撮影**：背面（料理・景色）→ 前面（自分）の2枚連続
- **投稿タイプ**：① 店舗投稿　② 自由投稿（キャンパス・空きコマ・景色）
- **キャプション**：撮影後にテキスト入力（最大140文字、Snapchat風オーバーレイ）
- **投稿場所**：どこからでもOK（家からでも）
- **投稿時間**：いつでもOK（時間制限なし）

---

## ⭐ スタンプ（確定数値）

| 状況 | 通常 | ランチタイム |
|------|------|----------|
| エリア内 店舗投稿 | **+5** | +10 |
| エリア内 自由投稿 | **+3** | +6 |
| エリア外 投稿 | **+1** | +2 |

- **ランチタイム**：11:30〜14:00 → すべて ×2
- **星獲得**：**投稿のたびにもらえる**（1日1回ではない）
- 星は累積していく
- エリア内外の判定 = 撮影時のGPS

---

## 🎓 単位・クーポン（確定ルール）

```
⭐ 10個 = 1単位 = クーポン1枚
```

- **クーポン獲得は 1日1枚まで**
- **獲得時：⭐を10だけ消費**（全リセットではない／残りは持ち越し）
- **クーポン有効期限：3日**（カウントダウン「あと◯時間」表示）
- クーポンはワンタップで使用 → 店員確認 → 即無効化

### クーポンの種類（店舗別に設定可能）

| ジャンル | 特典例 |
|---------|--------|
| ラーメン | 味玉・替え玉無料 |
| カフェ | ドリンク1杯無料 |
| 定食屋 | 小鉢サービス |
| 中華 | 餃子1皿無料 |

→ Firestoreで店舗ごとに管理。一律ではない。

---

## ⚡ チャンスタイム

- **1日1回ランダム発動**（11〜21時の間、平均50分に1回）
- **2分間有効**
- Push通知で知らせる
- 発動中は **エリア外でもエリア内扱い**（+5獲得）

---

## 🔒 エリア外ロック

| 自分の状態 | フィード閲覧 |
|----------|-----------|
| エリア内 | ✅ 常時OK |
| エリア外・投稿済み（24h以内） | ✅ OK |
| エリア外・未投稿 | 🔒 ロック（「投稿して解除」モーダル） |

---

## 👻 isInArea（投稿が消える仕組み）

```
投稿者が早稲田エリア外に移動
　↓
その投稿が全員のフィード・マップから自動的に非表示
　↓
ただしFirestore上では24時間は保持（再入場で復活）
```

- アプリ起動時にGPSチェック → `userStatus/{userId}.isInArea` を更新
- バックグラウンド追跡はしない（バッテリー対策）

---

## ⏳ 投稿の寿命

- 投稿は **24時間で自動削除**
- React版：Firebase Scheduled Functions で削除
- HTML版：クライアント側で best-effort 削除

---

## 👁 表示ルール

- **フィードはフレンドのみ**（知らない人の投稿は出ない）
- **前面カメラ（顔）はフレンドのみ閲覧可**　非フレンドは背面（料理）のみ
- 店舗投稿 + 自由投稿を統合、時系列表示
- いいね ❤️ / コメント 💬（リアルタイム同期）

---

## 🗺 マップ

- お店マーカー（混雑情報付き）
- 投稿の写真ピン（撮影位置のGPS座標）
- フレンドのみ閲覧可能
- isInArea で投稿者がエリア外なら、ピンも消える

---

## 🏫 エリア定義

```
中心： lat 35.7087, lng 139.7196（早稲田）
半径： 1.8km（本番）
　　　 ※太陽のHTML版は現在デバッグで50kmに拡大中。本番は1.8km。
```

---

## ☁ インフラ

| | HTML版（太陽） | React版（諸富） |
|---|--------------|---------------|
| 画像保存 | Firebase Storage | Cloudinary（諸富の判断でOK） |
| サーバー処理 | クライアント側 | Cloud Functions |
| 認証 | Firebase Auth（匿名+Google） | 同じ |
| DB | Firestore | 同じ |

→ **画像保存先・サーバー処理は実装が違ってOK。**
→ ただし **Firestoreのスキーマ（フィールド名）は統一する。** 下記参照。

---

## 🧱 Firestore スキーマ（統一必須）

### `photos` / `freePhotos`（投稿）
```
storeId, storeName, lat, lng,
postLat, postLng,          // 撮影位置（マップピン用）
photoBackURL, photoFrontURL,
caption,                   // キャプション（140字）
userId, userName, timestamp,
likes: [], likeCount, commentCount,
lunchBadge: bool,          // ランチタイム投稿フラグ
reports, hidden
```

### `photos/{id}/comments`（コメント）
```
userId, userName, text, createdAt
```

### `userStatus/{userId}`（在エリア管理）
```
isInArea: bool, lastChecked
```

### `coupons/{id}`（クーポン）
```
userId, type, name, emoji, desc,
storeId,                   // 店舗別クーポンの場合
issuedAt, expiresAt, used
```

### `recruits`（飯仲間募集）
```
userId, userName, zone, genre, time, note, createdAt, expiresAt
```

---

## 実装済み（太陽HTML版・検証済み）

- ✅ BeReal式撮影
- ✅ ホームフィード（photos + freePhotos 統合）
- ✅ いいね・コメント
- ✅ エリア外ロック
- ✅ isInArea
- ✅ キャプション
- ✅ ランチタイム判定 + スタンプ倍率
- ✅ GPS最寄り店自動提案
- ✅ チャンスタイム
- ✅ 単位・クーポン制度

## 今後の拡張（両者で実装）

- ⏳ マップ写真ピン
- ⏳ 店舗別クーポン（Firestore管理）
- ⏳ 来店判定（GPS+滞在時間+写真解析、MVPはGPSのみ）
- ⏳ 大隈講堂スポットボーナス
- ⏳ AI画像認識（将来）
- ⏳ Push通知

---

## 諸富への依頼

1. **星まわりの数字をこの仕様に合わせてほしい**
   - 1単位 = ⭐10（⭐5ではない）
   - エリア内投稿 = +5（⭐2ではない）
   - 星獲得 = 投稿ごと（1日1回ではない）
   - クーポン交換 = ⭐10消費（全リセットではない）

2. **Firestoreスキーマを上記に統一**

3. **画像保存・サーバー処理は諸富の判断でOK**（Cloudinary / Cloud Functions 可）
